import { StyleSheet } from 'react-native';

export const blueColor = '#0A7FFC';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
    justifyContent: 'center',
    alignItems: 'center'
  }
});
